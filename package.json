{
  "name": "sketchybar-gray-ui",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build:all": "pnpm -r build:tauri",
    "install:all": "pnpm build:all && ./scripts/install.sh",
    "dev:settings": "pnpm --filter settings dev",
    "tauri:settings": "pnpm --filter settings tauri dev"
  },
  "devDependencies": {}
}
